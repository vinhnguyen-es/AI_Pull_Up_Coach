[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-pull-up-coach"
version = "0.1.0"
description = "AI Pull-Up Coach backend"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["backend"]
